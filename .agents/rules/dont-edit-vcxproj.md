---
trigger: always_on
---

This is a Source SDK project, all edits (even quick ones) need to be done through VPC via editing the corresponding .vpc file, and then by running src\createallprojects.bat directly, to create mapping_tools.sln and all associated .vcxproj files.