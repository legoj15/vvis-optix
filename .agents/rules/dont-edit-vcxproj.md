---
trigger: always_on
---

This is a Source SDK project, all edits (even quick ones) need to be done through VPC via editing the corresponding .vpc file, and then by running src\createvvisprojects.bat directly, to create vvis.sln and all associated .vcxproj files.